import React from 'react'

const JSXRules = () => {
  return (
    <div>
        <h1>JSX Rules</h1>
        <p>JSX must return a single parent element</p>
        <p>JSX must be properly closed</p>
        <p>JSX attributes ares written using camelCase(e.g <strong>className</strong> instead of <strong>class</strong>)</p>
    </div>
  )
}

export default JSXRules